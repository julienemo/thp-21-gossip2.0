<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">

</head>

<body>
  <div class = "body">
    <h1>User details</h1><br /><br />
    <div class="card" style="width: 18rem;">
      <%u = User.find(@id)%>
      <div class="card-header">
        ID <%= @id%>: <%= u.first_name%> <%= u.last_name%> (<%= u.age%> yo)
      </div>
      <ul class="list-group list-group-flush">
        <li class="list-group-item"><%= u.email%></li>
        <li class="list-group-item">From <a href="/mycity"><%= u.city.name%></a></li>
        <li class="list-group-item">author of
          <span class="badge badge-pill badge-dark"><%= u.gossips.length%></span>
           gossips
        </li>
        <%if u.description == ""%>
          <% display = "This user didn't describe himself or herself."%>
        <%else%>
          <% display = u.description%>
        <%end%>
        <li class="list-group-item">Description: <%= display%></li>
      </ul>
    </div>
  </div>
</body>
