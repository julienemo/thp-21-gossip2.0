<h1>Welcome to the Gossip Project 2.0</h1>
<div>
  <p>New features:
    <ul>
      <li>We are now on Postgresql</li>
      <li>We have a seeded db</li>
      <li>We are also on rails</li>
    </ul>
  </p>
</div>

<p>Have a look at our burning hot GOSSIPS !</p>
  <div class = "list">
  <% Gossip.all.each_with_index do |g,i|%>
  <% stamp = g.created_at.to_date%>
    <%=i+1 %>
    <div class = "timestamp">On <%= stamp.to_date%>, at <%= stamp.to_s(:time)%> </div>
    <div class = "username"><%= User.find(g.user_id).first_name%> says</div>
    <div class = "gossip_content"><%= g.content%></div><br>

  <% end %>
</div>
