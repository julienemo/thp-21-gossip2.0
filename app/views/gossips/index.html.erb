<!DOCTYPE html>
<html lang="en">
<head>
  <!--Bootstrap theme link and navbar in layouts/application.html.erb-->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
</head>

<body>
  <div class = "body">
    <h1>Welcome to the Gossip Project 2.0</h1>
    <div>
      <p>New features:
        <ul>
          <li>We are now on Postgresql</li>
          <li>We have a seeded db</li>
          <li>We are also on rails</li>
        </ul>
      </p>
    </div>
    <% n = Gossip.all.length%>
    <% if current_user %>
      <p>Have a look at our <%= n%> burning hottt GOSSIPS or <a href="/gossips/new">create one of your own</a>!</p>
    <% else %>
      <p>Have a look at our <%= n%> burning hot GOSSIPS or <a href="/sessions">log in</a> to create your own!</p>
    <% end %>

    <p><br><br></p
    <% gossips = Gossip.all%>

    <div class = "d-flex flex-wrap flex-wrap-reverse justify-content-around">
      <% gossips.each do |g| %>
        <% stamp = g.created_at.to_date %>
        <div class="card col-md-3.05">
          <div class="card-body d-flex flex-column justify-content-between">
            <div class= 'col'>
              <h5 class="card-title">"<%= g.content %>"</h5>
              <p  class="card-text">Gossip <%= g.id %>

                <span class="badge badge-primary badge-pill">14</span>

              </p>
            </div>

            <!--all divs have 'flex-grow-1' by default-->
            <!--in vertical alignment they grow proportionally to take all available place-->
            <!--had to overwrite this for the button so that it stays at the bottom (with justify-content-between)-->
            <!--therefore aligning postions of all buttons-->
            <!--for other alignments go check application.css-->
            <div class = 'col flex-grow-0'>
              <a href="/gossips/<%=g.id %>" class="btn btn-primary w-100">See Gossip</a>
            </div>
          </div>
        </div>
      <% end %>



    </div>
  </div>
</body>
