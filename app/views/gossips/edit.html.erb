<!DOCTYPE html>
<html lang="en">
<head>
  <!--Bootstrap theme link and navbar in layouts/application.html.erb-->
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
</head>

<body>
  <div class = "body">
    <div>
      <h1>Modify gossip n°<%= @id%></h1>
      <%= form_tag url_for(action: 'update'), method: "put" do %>
        <%= label_tag 'Title (Mandatory, 3-14 characters)' %><br />
        <%= text_field_tag 'title', @gossip.title, class: 'form-control', style: 'width:30%' %>
        <br /> <br />
        <%= label_tag 'Content (Mandatory, 3-140 characters)' %><br />
        <%= text_area_tag 'content', @gossip.content, class: 'form-control'%>
        <br /> <br />
        <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
        <%= submit_tag "Submit", class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</body>
